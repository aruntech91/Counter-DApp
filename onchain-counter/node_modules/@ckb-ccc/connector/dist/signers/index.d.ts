import { ccc } from "@ckb-ccc/ccc";
import { ReactiveControllerHost } from "lit";
export declare class SignersController {
    private readonly host;
    wallets: ccc.WalletWithSigners[];
    private readonly defaultController;
    get controller(): ccc.SignersController;
    constructor(host: ReactiveControllerHost & {
        client: ccc.Client;
        signerFilter?: (signerInfo: ccc.SignerInfo, wallet: ccc.Wallet) => Promise<boolean>;
        preferredNetworks?: ccc.NetworkPreference[];
        name?: string;
        icon?: string;
        refreshSigner: () => void;
        signersController?: ccc.SignersController;
    });
    refresh(): Promise<void>;
    update(): void;
    hostConnected(): void;
    hostDisconnected(): void;
}
//# sourceMappingURL=index.d.ts.map